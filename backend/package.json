{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "node dist/server.js",
    "seed:admin": "ts-node-dev --transpile-only src/seedSuperAdmin.ts",
    "migrate:students:placement": "ts-node-dev --transpile-only src/scripts/migrations/backfillStudentPlacement.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "npm run clean && npm run build && node --test $(find dist -name \"*.test.js\")",
    "ci:check": "npm run lint && npm run typecheck && npm run test",
    "seed:tenant:enrollment-demo": "ts-node-dev --transpile-only src/scripts/seedTenantEnrollmentDemo.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.1",
    "morgan": "^1.10.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
